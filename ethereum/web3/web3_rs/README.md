# README

## testing

```
// case-1: MetaMask account + ETH ropsten testnet
$ cargo run --example get_transaction_count

// case-2: Local testing
$ (terminal-1) ganache-cli -b 3 -m "hamster coin cup brief quote trick stove draft hobby strong caught unable"
$ (terminal-2) cargo run --example contract_registration
```
